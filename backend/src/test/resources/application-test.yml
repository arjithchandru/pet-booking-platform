spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/petcare_db
    username: petcare_user
    password: petcare_password
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true